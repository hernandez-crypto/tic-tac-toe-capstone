CREATE TABLE BOARD (
    GAME_ID INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    DATE_CREATED TIMESTAMP DEFAULT now() NOT NULL,
    DATE_ENDED TIMESTAMP DEFAULT now() NOT NULL,
    PLAYER_STARTED_ID INTEGER NOT NULL,
    PLAYER_JOINED_ID INTEGER NOT NULL,
    SQUARE_one INTEGER NOT NULL,
    SQUARE_two INTEGER NOT NULL,
    SQUARE_three INTEGER NOT NULL,
    SQUARE_four INTEGER NOT NULL,
    SQUARE_five INTEGER NOT NULL,
    SQUARE_six INTEGER NOT NULL,
    SQUARE_seven INTEGER NOT NULL,
    SQUARE_eight INTEGER NOT NULL,
    SQUARE_nine INTEGER NOT NULL,
);


